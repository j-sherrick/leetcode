#include <stdio.h>

int lengthOfLongestSubstring(char* s) {
    size_t lengthOfLongest = 0, currentLength = 0;
    char* left = s;
    char* right = (s + 1);
    while ( *right != '\0') {
      if (*left == *right)
      {
      }
      
    }
}